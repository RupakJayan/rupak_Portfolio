import { motion } from 'framer-motion';
import { styles } from '../styles'; // Make sure you have styles defined if you're using them
import { ComputersCanvas } from './canvas'; // Ensure this component is correctly implemented

const Hero = () => {
  return (
    <section className='relative w-full h-screen mx-auto bg-hero-pattern bg-cover bg-center'>
    </section>
  );
}

export default Hero;
